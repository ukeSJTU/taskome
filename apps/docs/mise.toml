[tasks.dev]
description = "Run docs in development mode"
hide = true
run = "pnpm run dev"

[tasks.build]
description = "Build docs for production"
hide = true
run = "pnpm run build"

[tasks.start]
description = "Run the production docs server"
hide = true
run = "pnpm run start"

[tasks."check-types"]
description = "Type-check docs"
hide = true
run = "pnpm run check-types"
