# Docker Compose uses "password" when this is unset. Set a different local value if needed.
POSTGRES_PASSWORD=password
# Browser origins allowed to send direct presigned PUT/GET requests to SeaweedFS.
SEAWEEDFS_ALLOWED_ORIGINS=http://localhost:3000
