# Build tasks: XDenovo frontend visual language

Generated from: `.design/frontend-visual-language/DESIGN_BRIEF.md`
Date: 2026-08-25

## Build boundary

Phase 6 delivers one production-quality English page in `apps/web`. It adopts the shared base and Web token layers, but it does not import `console.css` or change any file under `apps/console`.

The page uses server-rendered HTML for all essential content. Client-side code is limited to the accessible mobile navigation. This build documents the future English and Simplified Chinese route model without installing i18n or publishing translated routes. It also keeps the current two-dimensional scientific visual behind a replaceable presentation seam; Three.js remains future work.

## Foundation and first viewport

- [x] **Deliver the XDenovo first viewport and establish the visual direction**: Replace the placeholder with a responsive page shell, skip link, brand Header, editorial Hero, signal-orange **Sign in** action, **Docs** destination, and a lightweight SVG/CSS protein-compute visual. Connect the approved `base.css + web.css` layers through stable `@taskome/ui` exports; activate `data-surface="web"` and the explicit light theme; load only the used Latin weights of Newsreader, IBM Plex Sans, and IBM Plex Mono; add optimized full-color and single-color treatments of the approved archived mark; and resolve Web, Docs, and Console origins through one documented site-config seam with local development defaults rather than guessed production hosts. The first viewport is done when it identifies XDenovo and Taskome, exposes Sign in without scrolling, retains meaningful content without animation or JavaScript, and works at 375 px and wide desktop widths. _Modifies: `packages/ui/package.json`, `apps/web/src/app/layout.tsx`, `apps/web/src/app/page.tsx`, `apps/web/.env.example`, and `apps/web/README.md`. Creates: Web-local brand, Header, Hero, scientific-visual, site-config, and asset modules. Reuses as-is: `@taskome/ui` Sheet interaction behavior and the approved token files. Sign-in destinations use styled anchors to preserve link semantics; Web-specific classes provide the editorial radius and action treatment without changing the shared Console-facing Button primitive._

## Core narrative

- [x] **Explain why protein-design compute needs Taskome**: Add the Approach and Taskome sections in semantic reading order, using an editorial section-heading composition and a responsive scientific workflow diagram to connect curated Tools, immutable Jobs, execution Attempts, Job Outputs, Projects, files, and browser Utilities without depicting Taskome as an interactive demo. Add the `#taskome` Header destination only when its section exists. The slice is done when a researcher can understand the problem, the durable provenance model, and Taskome's role from rendered HTML at mobile and desktop widths. _Depends on: Deliver the XDenovo first viewport. Creates: Web-local section-heading, Approach, Taskome-story, and domain-model visual components. Modifies: marketing Header and home-page composition. Reuses as-is: Web tokens, anchor-link semantics, and canonical vocabulary from `CONTEXT.md`; no shared UI component changes._

- [x] **Show one Taskome model across four access channels**: Add the Access section as an ordered editorial composition for Web App, MCP Agent, Direct API Client, and CLI, with concise examples or annotations that reinforce identical Taskome concepts rather than presenting four separate products. Keep essential labels and relationships in HTML; treat any connecting SVG traces as decorative. The slice is done when the four journeys scan clearly at 375 px, transform into a deliberate desktop composition, and introduce no second CTA. _Depends on: Explain why protein-design compute needs Taskome. Creates: Web-local Access section and access-channel composition. Modifies: home-page composition. Reuses as-is: section heading, editorial index treatment, mono typography tokens, and Taskome domain language._

- [x] **Complete the credibility and conversion arc**: Add Capabilities, Company, final sign-in prompt, and Footer sections using the approved facts and contact or registration details from `references/old-website`, edited only for hierarchy and concise English. Condense peptide and antibody design, PDC/PRC, antimicrobial, cosmetic, and custom protein design into one scientific-capabilities narrative instead of restoring the archived product grid. Add `#capabilities` and `#company` Header destinations when those sections exist, repeat only the existing **Sign in** action at the close, and keep legal destinations in the Footer. The slice is done when the full page follows the approved narrative, every Header link resolves, and the Footer contains only real destinations and approved facts. _Depends on: Show one Taskome model across four access channels. Creates: Web-local Capabilities, Company, final-prompt, and Footer components. Modifies: marketing Header and home-page composition. Reuses: section heading, Sign in action, XDenovo mark, Web tokens, and archived content as a read-only factual source; does not reuse the archived dark-neon presentation or card grid._

## Search and sharing

- [x] **Ship the English page as a discoverable document**: Add useful page metadata, canonical URL handling, Open Graph output, `robots.txt`, sitemap output, and factual Organization plus SoftwareApplication structured data. Extend the site-config seam with the public Web origin, keep Console and Docs links on their own origins, and document the required production environment values without hard-coding unknown hosts. The slice is done when the production build emits valid crawl and sharing surfaces, the canonical remains `/`, structured data makes no unsupported scientific or commercial claims, and the implementation records—but does not wire—the future `/en/...` and `/zh-cn/...` SEO contract already defined in the IA. _Depends on: Complete the credibility and conversion arc. Creates: App Router metadata, robots, sitemap, Open Graph, and structured-data modules. Modifies: root layout, home page, site config, `apps/web/.env.example`, and `apps/web/README.md`. Reuses: Next.js metadata APIs and the final page copy; no i18n dependency or translated route is created._

## Responsive and quality pass

- [x] **Validate the complete Web experience against the brief**: Audit and refine the whole page at 375 px, 768 px, 1280 px, and a wide desktop viewport; verify semantic landmarks and heading order, keyboard navigation, mobile-menu focus entry and restoration, visible focus on every surface, 44 by 44 CSS-pixel primary touch targets, sticky-Header anchor offsets, WCAG 2.2 AA contrast after final compositing, reduced motion, informative-graphic text equivalents, and content resilience when images, custom fonts, animation, or JavaScript are unavailable. Apply the installed Web design guidelines during this pass, keep server components as the default, confirm that no WebGL or unnecessary dependency entered the bundle, and run `mise run //packages/ui:check`, `mise run //apps/web:check`, `mise run //apps/web:build`, formatter checks for changed files, and `git diff --check`. The task is done when browser inspection shows no clipped content, unintended horizontal scrolling, dead links, missing focus, or essential animation-only meaning, and all required checks pass. _Depends on: Ship the English page as a discoverable document. Modifies: any Web-local component or style that fails the audit. Reuses: the complete page and repository checks. Creates only targeted accessibility or fallback code when the audit demonstrates a need._

## Review

- [ ] **Design review**: After Phase 6 is complete, run `/design-review` only when explicitly requested. Compare screenshots and behavior against the brief at desktop, tablet, and 375 px mobile widths; save the review and evidence under `.design/frontend-visual-language/`. _Reuses: completed Web implementation, design brief, information architecture, and token specification. Creates: review artifact and screenshots; does not modify Console._
