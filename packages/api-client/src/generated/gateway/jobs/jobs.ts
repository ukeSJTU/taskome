/**
 * Generated by orval 🍺
 * Do not edit manually.
 * taskome-gateway
 * OpenAPI spec version: 0.1.0
 */
import type {
  CreateJobRequest,
  GetJobPathParameters,
  JobListResponse,
  JobResponse,
  ListJobsParams,
} from "../models";

import { gatewayFetch } from "../../../mutator";

export const getListJobsUrl = (params?: ListJobsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/jobs?${stringifiedParams}` : `/jobs`;
};

/**
 * List the caller's own Jobs, newest first.
 * @summary List Jobs
 */
export const listJobs = async (
  params?: ListJobsParams,
  options?: Parameters<typeof gatewayFetch>[1],
): Promise<JobListResponse> => {
  return gatewayFetch<JobListResponse>(getListJobsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getCreateJobUrl = () => {
  return `/jobs`;
};

/**
 * Validate and durably queue a Job, dispatching it in the background.
 * @summary Create Job
 */
export const createJob = async (
  createJobRequest: CreateJobRequest,
  options?: Parameters<typeof gatewayFetch>[1],
): Promise<JobResponse> => {
  return gatewayFetch<JobResponse>(getCreateJobUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(createJobRequest),
  });
};

export const getGetJobUrl = ({ jobId }: GetJobPathParameters) => {
  return `/jobs/${encodeURIComponent(String(jobId))}`;
};

/**
 * Return a caller-owned Job's current status and result.
 * @summary Get Job
 */
export const getJob = async (
  { jobId }: GetJobPathParameters,
  options?: Parameters<typeof gatewayFetch>[1],
): Promise<JobResponse> => {
  return gatewayFetch<JobResponse>(getGetJobUrl({ jobId }), {
    ...options,
    method: "GET",
  });
};
