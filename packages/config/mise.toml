[tasks.lint]
description = "Apply safe lint fixes to shared configuration"
run = "pnpm run lint"

[tasks.check]
description = "Run read-only shared configuration lint and type checks"
run = ["pnpm run lint:check", "pnpm run check-types"]
