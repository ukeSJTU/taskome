# infra/proxy/

Reverse proxy and TLS termination for the production deployment (referenced in `apps/gateway/README.md`: "TLS termination is expected at the reverse proxy").

Placeholder — no target machine, domain, or certificate approach has been decided yet. Fill this in when a real deployment target exists instead of designing against an assumption now.
