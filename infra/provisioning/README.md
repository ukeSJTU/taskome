# infra/provisioning/

Setup scripts for the dedicated GPU server that multi-node GPU scheduling will eventually need (Docker, NVIDIA Container Toolkit, GPU drivers, etc.) — see [`docs/architecture/constraints.md`](../../docs/architecture/constraints.md) for why single-machine deployment is deliberately deferred.

Placeholder — no target machine exists yet. Fill this in once a real machine is provisioned instead of scripting against an assumption now.
